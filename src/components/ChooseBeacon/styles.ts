import { ScaledSheet } from '../../utils';

const styles = ScaledSheet.create({
  contentContainer: {
    flex: 1,
    padding: '30@s'
  },

  listContainer: {
    height: '270@s',
    marginBottom: '35@s'
  }
});

export default styles;
