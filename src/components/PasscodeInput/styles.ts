import { ScaledSheet } from '../../utils';

const styles = ScaledSheet.create({
  digitsContainer: {
    flexDirection: 'row'
  },

  input: {
    height: 0,
    opacity: 0
  }
});

export default styles;
