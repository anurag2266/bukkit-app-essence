import { ScaledSheet } from '../../../utils';

const styles = ScaledSheet.create({
  container: {
    flex: 1,
    paddingTop: '35@s',
    paddingLeft: '60@s',
    paddingRight: '128@s'
  },

  fingerprintButton: {
    marginTop: '99@s',
    marginBottom: '148@s'
  }
});

export default styles;
