import { ScaledSheet } from '../../../utils';

const styles = ScaledSheet.create({
  container: {
    flex: 1,
    padding: '30@s',
    paddingTop: '35@s'
  },

  introText: {
    marginLeft: '30@s',
    marginRight: '60@s',
    marginBottom: '125@s'
  },
  emailInput: {
    marginTop: '0@s',
    marginBottom: '30@s'
  },
});

export default styles;
