import { ScaledSheet } from '../../../utils';

const styles = ScaledSheet.create({
  container: {
    flex: 1,
    paddingTop: '35@s',
    paddingLeft: '60@s',
    paddingRight: '39@s'
  },

  introText: {
    marginBottom: '83@s'
  }
});

export default styles;
